/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetContactsIdEventsQueryResponse,
  GetContactsIdEventsPathParams,
  GetContactsIdEvents401,
  GetContactsIdEvents404,
} from "../types/GetContactsIdEvents.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/http-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getContactsIdEvents } from "../clients/getContactsIdEvents.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getContactsIdEventsSuspenseQueryKey = (
  id: GetContactsIdEventsPathParams["id"] | undefined,
) => [{ url: "/contacts/:id/events", params: { id: id } }] as const;

export type GetContactsIdEventsSuspenseQueryKey = ReturnType<
  typeof getContactsIdEventsSuspenseQueryKey
>;

export function getContactsIdEventsSuspenseQueryOptions(
  id: GetContactsIdEventsPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getContactsIdEventsSuspenseQueryKey(id);
  return queryOptions<
    GetContactsIdEventsQueryResponse,
    ResponseErrorConfig<GetContactsIdEvents401 | GetContactsIdEvents404>,
    GetContactsIdEventsQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getContactsIdEvents(id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List events for a contact
 * {@link /contacts/:id/events}
 */
export function useGetContactsIdEventsSuspense<
  TData = GetContactsIdEventsQueryResponse,
  TQueryKey extends QueryKey = GetContactsIdEventsSuspenseQueryKey,
>(
  id: GetContactsIdEventsPathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetContactsIdEventsQueryResponse,
        ResponseErrorConfig<GetContactsIdEvents401 | GetContactsIdEvents404>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getContactsIdEventsSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...getContactsIdEventsSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<GetContactsIdEvents401 | GetContactsIdEvents404>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
