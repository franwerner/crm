/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetProjectsQueryResponse,
  GetProjectsQueryParams,
  GetProjects401,
} from "../types/GetProjects.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/utils/http-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getProjects } from "../clients/getProjects.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getProjectsQueryKey = (params?: GetProjectsQueryParams) =>
  [{ url: "/projects" }, ...(params ? [params] : [])] as const;

export type GetProjectsQueryKey = ReturnType<typeof getProjectsQueryKey>;

export function getProjectsQueryOptions(
  params?: GetProjectsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getProjectsQueryKey(params);
  return queryOptions<
    GetProjectsQueryResponse,
    ResponseErrorConfig<GetProjects401>,
    GetProjectsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getProjects(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List projects
 * {@link /projects}
 */
export function useGetProjects<
  TData = GetProjectsQueryResponse,
  TQueryData = GetProjectsQueryResponse,
  TQueryKey extends QueryKey = GetProjectsQueryKey,
>(
  params?: GetProjectsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetProjectsQueryResponse,
        ResponseErrorConfig<GetProjects401>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getProjectsQueryKey(params);

  const query = useQuery(
    {
      ...getProjectsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetProjects401>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
