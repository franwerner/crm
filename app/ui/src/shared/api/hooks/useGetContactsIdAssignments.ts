/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetContactsIdAssignmentsQueryResponse,
  GetContactsIdAssignmentsPathParams,
  GetContactsIdAssignments401,
  GetContactsIdAssignments404,
} from "../types/GetContactsIdAssignments.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/utils/http-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getContactsIdAssignments } from "../clients/getContactsIdAssignments.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getContactsIdAssignmentsQueryKey = (
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
) => [{ url: "/contacts/:id/assignments", params: { id: id } }] as const;

export type GetContactsIdAssignmentsQueryKey = ReturnType<
  typeof getContactsIdAssignmentsQueryKey
>;

export function getContactsIdAssignmentsQueryOptions(
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getContactsIdAssignmentsQueryKey(id);
  return queryOptions<
    GetContactsIdAssignmentsQueryResponse,
    ResponseErrorConfig<
      GetContactsIdAssignments401 | GetContactsIdAssignments404
    >,
    GetContactsIdAssignmentsQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getContactsIdAssignments(id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List assignments (assignees) of a contact
 * {@link /contacts/:id/assignments}
 */
export function useGetContactsIdAssignments<
  TData = GetContactsIdAssignmentsQueryResponse,
  TQueryData = GetContactsIdAssignmentsQueryResponse,
  TQueryKey extends QueryKey = GetContactsIdAssignmentsQueryKey,
>(
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetContactsIdAssignmentsQueryResponse,
        ResponseErrorConfig<
          GetContactsIdAssignments401 | GetContactsIdAssignments404
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getContactsIdAssignmentsQueryKey(id);

  const query = useQuery(
    {
      ...getContactsIdAssignmentsQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      GetContactsIdAssignments401 | GetContactsIdAssignments404
    >
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
