/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetContactsIdAssignmentsQueryResponse,
  GetContactsIdAssignmentsPathParams,
  GetContactsIdAssignments401,
  GetContactsIdAssignments404,
} from "../types/GetContactsIdAssignments.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/utils/http-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getContactsIdAssignments } from "../clients/getContactsIdAssignments.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getContactsIdAssignmentsSuspenseQueryKey = (
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
) => [{ url: "/contacts/:id/assignments", params: { id: id } }] as const;

export type GetContactsIdAssignmentsSuspenseQueryKey = ReturnType<
  typeof getContactsIdAssignmentsSuspenseQueryKey
>;

export function getContactsIdAssignmentsSuspenseQueryOptions(
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getContactsIdAssignmentsSuspenseQueryKey(id);
  return queryOptions<
    GetContactsIdAssignmentsQueryResponse,
    ResponseErrorConfig<
      GetContactsIdAssignments401 | GetContactsIdAssignments404
    >,
    GetContactsIdAssignmentsQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getContactsIdAssignments(id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List assignments (assignees) of a contact
 * {@link /contacts/:id/assignments}
 */
export function useGetContactsIdAssignmentsSuspense<
  TData = GetContactsIdAssignmentsQueryResponse,
  TQueryKey extends QueryKey = GetContactsIdAssignmentsSuspenseQueryKey,
>(
  id: GetContactsIdAssignmentsPathParams["id"] | undefined,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetContactsIdAssignmentsQueryResponse,
        ResponseErrorConfig<
          GetContactsIdAssignments401 | GetContactsIdAssignments404
        >,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getContactsIdAssignmentsSuspenseQueryKey(id);

  const query = useSuspenseQuery(
    {
      ...getContactsIdAssignmentsSuspenseQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<
    TData,
    ResponseErrorConfig<
      GetContactsIdAssignments401 | GetContactsIdAssignments404
    >
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
