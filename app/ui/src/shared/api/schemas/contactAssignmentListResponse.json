{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "userId": {
            "type": "string",
            "description": "UUID of the assigned user"
          },
          "userName": {
            "type": "string",
            "description": "Display name of the assigned user",
            "example": "Jane Doe"
          },
          "role": {
            "type": "string",
            "enum": ["Owner", "Collaborator"],
            "example": "Owner"
          },
          "assignedBy": {
            "type": "string",
            "description": "UUID of the user that performed the assignment"
          },
          "assignedAt": {
            "type": "string",
            "description": "ISO 8601 timestamp",
            "example": "2024-01-01T00:00:00.000Z"
          }
        },
        "required": ["userId", "userName", "role", "assignedBy", "assignedAt"],
        "x-readme-ref-name": "ContactAssignmentListItem"
      },
      "description": "List of items"
    },
    "total": {
      "type": "integer",
      "description": "Total number of items",
      "example": 0
    },
    "limit": { "type": "integer", "description": "Page size", "example": 20 },
    "offset": { "type": "integer", "description": "Page offset", "example": 0 }
  },
  "required": ["items", "total", "limit", "offset"],
  "x-readme-ref-name": "ContactAssignmentListResponse"
}
