/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4";

export const contactListItemSchema = z.object({
  id: z.string().describe("Contact UUID"),
  name: z.string(),
  contactType: z.enum(["Person", "Company"]),
  sex: z.nullable(z.enum(["Male", "Female", "Other", "Unspecified"])),
  notes: z.nullable(z.string()),
  pipelineState: z.enum(["Contact", "Lead", "AtRisk", "Customer", "Discarded"]),
  sourceChannel: z.nullable(
    z.enum(["Instagram", "WhatsApp", "Referral", "Email", "Other"]),
  ),
  interestLevel: z.nullable(z.enum(["Cold", "Warm", "Hot"])),
  createdBy: z.string().describe("User UUID who created this contact"),
  creator: z.nullable(
    z
      .object({
        id: z.string(),
        name: z.string(),
      })
      .describe(
        "Creator user data. Null if the creator user no longer exists.",
      ),
  ),
  primaryChannel: z.nullable(
    z
      .object({
        channelType: z.enum([
          "Phone",
          "Email",
          "WhatsApp",
          "Instagram",
          "Website",
          "Other",
        ]),
        value: z.string(),
      })
      .describe("Primary communication channel"),
  ),
  createdAt: z.string().describe("ISO 8601 timestamp"),
  updatedAt: z.string().describe("ISO 8601 timestamp"),
});
