{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "Budget item UUID" },
          "projectId": { "type": "string", "description": "Project UUID" },
          "concept": { "type": "string", "example": "Design phase" },
          "amountMinor": {
            "type": "number",
            "description": "Amount in minor currency units",
            "example": 150000
          },
          "createdAt": {
            "type": "string",
            "description": "ISO 8601 timestamp"
          },
          "updatedAt": { "type": "string", "description": "ISO 8601 timestamp" }
        },
        "required": [
          "id",
          "projectId",
          "concept",
          "amountMinor",
          "createdAt",
          "updatedAt"
        ],
        "x-readme-ref-name": "ProjectBudgetItemView"
      },
      "description": "List of items"
    },
    "total": {
      "type": "integer",
      "description": "Total number of items",
      "example": 0
    },
    "limit": { "type": "integer", "description": "Page size", "example": 20 },
    "offset": { "type": "integer", "description": "Page offset", "example": 0 }
  },
  "required": ["items", "total", "limit", "offset"],
  "x-readme-ref-name": "ProjectBudgetItemListResponse"
}
