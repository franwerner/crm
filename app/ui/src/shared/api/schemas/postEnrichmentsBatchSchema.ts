/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { problemSchema } from "./problemSchema.ts";
import { z } from "zod/v4";

/**
 * @description Batch enrichment queued. Returns created insight IDs (duplicates skipped).
 */
export const postEnrichmentsBatch201Schema = z.object({
  insightIds: z.array(z.uuid()),
  count: z.int(),
  skipped: z.optional(z.int()),
  exceededMax: z.optional(z.boolean()),
});

/**
 * @description Validation error.
 */
export const postEnrichmentsBatch400Schema = z.lazy(() => problemSchema);

/**
 * @description Unauthorized.
 */
export const postEnrichmentsBatch401Schema = z.lazy(() => problemSchema);

/**
 * @description Template inactive.
 */
export const postEnrichmentsBatch422Schema = z.lazy(() => problemSchema);

export const postEnrichmentsBatchMutationRequestSchema = z.union([
  z.object({
    kind: z.enum(["filter"]),
    filterGroups: z
      .array(
        z
          .array(
            z.object({
              field: z.string().min(1),
              op: z.enum([
                "eq",
                "ne",
                "in",
                "nin",
                "gt",
                "gte",
                "lt",
                "lte",
                "between",
                "ilike",
                "isNull",
                "isNotNull",
              ]),
              value: z.optional(
                z.union([
                  z.boolean(),
                  z.any(),
                  z.array(
                    z.union([z.boolean(), z.any(), z.string(), z.number()]),
                  ),
                  z.string(),
                  z.number(),
                ]),
              ),
            }),
          )
          .max(25),
      )
      .max(10),
    search: z.optional(z.string().min(1)),
    templateId: z.uuid(),
  }),
  z.object({
    kind: z.enum(["ids"]),
    contactIds: z.array(z.uuid()).min(1),
    templateId: z.uuid(),
  }),
]);

export const postEnrichmentsBatchMutationResponseSchema = z.lazy(
  () => postEnrichmentsBatch201Schema,
);
