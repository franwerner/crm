{
  "type": "object",
  "properties": {
    "importId": {
      "type": "string",
      "format": "uuid",
      "description": "UUID of the created import record",
      "example": "01938b0c-0000-7000-0000-000000000001"
    },
    "status": {
      "type": "string",
      "description": "Import status after upload (awaiting_mapping)",
      "example": "awaiting_mapping"
    },
    "columnHeaders": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Column headers detected from the first row of the uploaded file",
      "example": ["Full Name", "Email", "Phone"]
    }
  },
  "required": ["importId", "status", "columnHeaders"],
  "x-readme-ref-name": "ImportUploadResponse"
}
