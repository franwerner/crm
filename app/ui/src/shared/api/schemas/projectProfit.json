{
  "type": "object",
  "properties": {
    "amountMinor": {
      "type": "number",
      "description": "Signed amount in minor currency units (may be negative)",
      "example": -30000
    },
    "currency": {
      "type": "string",
      "description": "ISO 4217 currency code",
      "example": "USD"
    }
  },
  "required": ["amountMinor", "currency"],
  "x-readme-ref-name": "ProjectProfit"
}
