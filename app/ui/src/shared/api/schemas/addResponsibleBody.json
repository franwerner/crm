{
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "format": "uuid",
      "description": "UUID of the user being assigned",
      "example": "01938b0c-..."
    },
    "role": {
      "type": "string",
      "enum": ["Lead", "Member"],
      "description": "Responsible role",
      "example": "Member"
    }
  },
  "required": ["userId", "role"],
  "x-readme-ref-name": "AddResponsibleBody"
}
