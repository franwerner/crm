{
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "User email (unique)",
      "example": "jane@example.com"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "User full name",
      "example": "Jane Doe"
    },
    "password": {
      "type": "string",
      "minLength": 8,
      "description": "Password (>= 8 chars)",
      "example": "correct-horse"
    }
  },
  "required": ["email", "name", "password"],
  "x-readme-ref-name": "CreateUserBody"
}
