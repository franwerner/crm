{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "Document UUID" },
          "projectId": { "type": "string", "description": "Project UUID" },
          "fileName": {
            "type": "string",
            "description": "Sanitized file name",
            "example": "contrato-firmado.pdf"
          },
          "contentType": {
            "type": "string",
            "description": "MIME type",
            "example": "application/pdf"
          },
          "sizeBytes": {
            "type": "number",
            "description": "File size in bytes",
            "example": 204800
          },
          "uploadedBy": {
            "type": "string",
            "description": "User UUID who uploaded the document"
          },
          "uploadedAt": {
            "type": "string",
            "description": "ISO 8601 timestamp"
          },
          "createdAt": {
            "type": "string",
            "description": "ISO 8601 timestamp"
          },
          "updatedAt": { "type": "string", "description": "ISO 8601 timestamp" }
        },
        "required": [
          "id",
          "projectId",
          "fileName",
          "contentType",
          "sizeBytes",
          "uploadedBy",
          "uploadedAt",
          "createdAt",
          "updatedAt"
        ],
        "x-readme-ref-name": "ProjectDocumentView"
      },
      "description": "List of items"
    },
    "total": {
      "type": "integer",
      "description": "Total number of items",
      "example": 0
    },
    "limit": { "type": "integer", "description": "Page size", "example": 20 },
    "offset": { "type": "integer", "description": "Page offset", "example": 0 }
  },
  "required": ["items", "total", "limit", "offset"],
  "x-readme-ref-name": "ProjectDocumentListResponse"
}
