{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string", "description": "User UUID" },
          "email": { "type": "string", "example": "jane@example.com" },
          "name": { "type": "string", "example": "Jane Doe" },
          "createdAt": {
            "type": "string",
            "description": "ISO 8601 timestamp"
          },
          "updatedAt": { "type": "string", "description": "ISO 8601 timestamp" }
        },
        "required": ["id", "email", "name", "createdAt", "updatedAt"],
        "x-readme-ref-name": "UserView"
      },
      "description": "List of items"
    },
    "total": {
      "type": "integer",
      "description": "Total number of items",
      "example": 0
    },
    "limit": { "type": "integer", "description": "Page size", "example": 20 },
    "offset": { "type": "integer", "description": "Page offset", "example": 0 }
  },
  "required": ["items", "total", "limit", "offset"],
  "x-readme-ref-name": "UserListResponse"
}
