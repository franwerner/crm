/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@shared/lib/http-client";
import type {
  GetContactsIdStateChangesQueryResponse,
  GetContactsIdStateChangesPathParams,
  GetContactsIdStateChanges401,
  GetContactsIdStateChanges404,
} from "../types/GetContactsIdStateChanges.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/http-client";

function getGetContactsIdStateChangesUrl(
  id: GetContactsIdStateChangesPathParams["id"],
) {
  const res = { method: "GET", url: `/contacts/${id}/state-changes` as const };
  return res;
}

/**
 * @summary List state changes for a contact
 * {@link /contacts/:id/state-changes}
 */
export async function getContactsIdStateChanges(
  id: GetContactsIdStateChangesPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetContactsIdStateChangesQueryResponse,
    ResponseErrorConfig<
      GetContactsIdStateChanges401 | GetContactsIdStateChanges404
    >,
    unknown
  >({
    method: "GET",
    url: getGetContactsIdStateChangesUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
