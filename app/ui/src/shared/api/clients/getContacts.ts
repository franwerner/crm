/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@shared/lib/http-client";
import type {
  GetContactsQueryResponse,
  GetContactsQueryParams,
  GetContacts401,
} from "../types/GetContacts.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/http-client";

function getGetContactsUrl() {
  const res = { method: "GET", url: `/contacts` as const };
  return res;
}

/**
 * @summary List contacts
 * {@link /contacts}
 */
export async function getContacts(
  params?: GetContactsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetContactsQueryResponse,
    ResponseErrorConfig<GetContacts401>,
    unknown
  >({
    method: "GET",
    url: getGetContactsUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
