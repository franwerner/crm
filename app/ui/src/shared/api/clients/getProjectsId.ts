/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@shared/lib/utils/http-client";
import type {
  GetProjectsIdQueryResponse,
  GetProjectsIdPathParams,
  GetProjectsId401,
  GetProjectsId404,
} from "../types/GetProjectsId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@shared/lib/utils/http-client";

function getGetProjectsIdUrl(id: GetProjectsIdPathParams["id"]) {
  const res = { method: "GET", url: `/projects/${id}` as const };
  return res;
}

/**
 * @summary Get a project by id
 * {@link /projects/:id}
 */
export async function getProjectsId(
  id: GetProjectsIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetProjectsIdQueryResponse,
    ResponseErrorConfig<GetProjectsId401 | GetProjectsId404>,
    unknown
  >({
    method: "GET",
    url: getGetProjectsIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
